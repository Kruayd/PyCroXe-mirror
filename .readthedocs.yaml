version: 2

build:
  os: "ubuntu-lts-latest"
  tools:
    python: "latest"
  apt_packages:
    - mariadb-server
    - mariadb-client

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs

sphinx:
  configuration: docs/source/conf.py
